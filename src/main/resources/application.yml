spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/import_finance
    username: import
    password: import
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
